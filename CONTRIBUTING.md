# Contributing

Issues and pull requests are welcome. Keep changes focused, add tests for security-sensitive behavior, and run `npm run lint` and `npm run build` before submitting.

Do not commit `.env` files, credentials, webhook payloads, customer information, or monitoring exports.
